# SIXPM iPhone App Store Status

Last updated: 2026-08-18 (Pacific)  
Mission state: active  
Current phase: 6 — Accessibility and iPhone polish

## Mission

Ship SIXPM as a free, ad-free, account-free, iPhone-only App Store app for the United States. The web product remains React 19 + Vite; the native shell will use Capacitor 8 and Xcode 26, target iOS 17+, bundle its UI locally, and fetch only a versioned rights-gated catalog from the SIXPM Vercel deployment.

## Working locations

| Purpose | Location |
| --- | --- |
| Canonical repository | `/Users/jameswalker/Documents/AI Projects - All/10_ACTIVE_PROJECTS/Apps/X117` |
| iOS implementation worktree | `/Users/jameswalker/.codex/worktrees/X117-ios-app-store` |
| iOS implementation branch | `codex/ios-app-store` |
| Production web deployment | `https://sixpm.vercel.app/` |

The canonical checkout has two pre-existing protected changes: a modified `scripts/generate-bio.js` and untracked `AGENTS.md`. They have not been inspected, staged, copied, or changed by this mission.

## Gate log

| Gate | State | Evidence / next action |
| --- | --- | --- |
| 0.1 Repository and dirty-tree revalidation | passed | Protected canonical changes confirmed; iOS work occurs only in the clean Codex worktree. |
| 0.2 PR #11 revalidation and merge | passed | Mergeable, green checks, no human review threads; merged to `main` as `dc15dfa1dc3d78e2e871fe9c0389ff648571b2e9`. |
| 0.3 PR #12 revalidation | passed | Mergeable, green checks, no human review threads before merge. Its original stacked base remained `agent/amc-la-category`; its merge commit `4da559cfe34c6f9bbbd722a0c8b79a4140e14b3d` is not yet on `main`. |
| 0.4 PR #12 integration | passed | The iOS branch carries the reviewed source changes and a fresh cinema regeneration. Conflicting integration PR #13 was closed with an explanation after it was superseded by the resolved release lane. |
| 0.5 Production data reachability | partial — release blocker | `theaters.json`, `jazz-venues.json`, `restaurants.json`, and the application returned HTTP 200 on 2026-08-18; cinema feed timestamp: `2026-08-18T10:25:59.832Z`. That check did not prove the iOS catalog path. Fresh native QA later established that `https://sixpm.vercel.app/catalog/v1/index.json` currently returns the web app HTML (`text/html`), not a catalog. On revalidation, production was confirmed to run `main` commit `dc15dfa1`, whose tree does not contain `public/catalog/v1/`; the SPA fallback is therefore responding to a missing production artifact, not misrouting an existing file. The release branch's Vercel build copied all four catalog JSON files byte-for-byte into `.vercel/output/static/catalog/v1/`. A reviewed deployment must publish `catalog/v1/` before release; no deployment was performed. |
| 0.6 Native toolchain | passed | Xcode 26.6, iOS 26.5 iPhone simulator, and one Apple Development identity are present. Node 22.23.2 is installed at `/opt/homebrew/opt/node@22`; `.nvmrc` and `package.json` now pin the repository to Node 22. |
| 0.7 Web-wide strict-data check | warning — not the iOS catalog gate | On 2026-08-18, `npm run release:check` passed lint, 93 unit tests, production build, audit, 58 browser checks, and iOS catalog validation, then correctly exited 2 on three broader web-guide warnings: five monitored theatres have no future screenings; the legacy restaurant guide now has 2/232 coordinate/hour records after Google-derived fields were removed; and that broader guide’s usable dinner pool is therefore 2 records across 2 neighborhoods. The command is intentionally strict and now read-only. This does not waive the web warnings; the separate iPhone gate uses only the verified rights-gated catalog, whose check passes. |
| 0.8 Draft release lane | passed | Draft PR #14 (`codex/ios-app-store` → `main`) is the sole authoritative App Store mission lane. |
| 0.9 Release-lane revalidation | passed | On 2026-08-18, PR #14 remained draft, clean, and green in GitHub Quality plus Vercel Preview. PRs #11 and #12 are both `MERGED`; this release lane retains the reviewed #12 source. Node 22.23.2, Xcode 26.6, iOS 26.5 simulators, and one valid Apple Development identity were rechecked. `npm run check` passed (93 unit tests), `npm run test:e2e` passed (56 browser checks), privacy validation and Capacitor sync passed, and an iPhone Simulator build succeeded. The canonical checkout still contains the protected modified `scripts/generate-bio.js` and untracked `AGENTS.md`, untouched. |
| 0.10 Preview catalog reachability | partial, non-release evidence | The green Vercel preview contains the catalog build artifacts, but deployment protection redirects its catalog URL to Vercel SSO. It cannot serve as a device-accessible substitute for the public catalog. Production remains the only release endpoint and still returns SPA HTML at `/catalog/v1/index.json`; no deployment was performed. |
| 0.11 Physical-device signing readiness | owner/external gate | A connected owner iPhone 14 Pro Max was discovered and the project now declares the verified `GW78B8TZ7H` development team for Debug and Release. Simulator compilation still succeeds. A signed device build correctly reaches provisioning but Xcode reports no signed-in Apple Developer account and no profile for `com.xae117.sixpm`; automatic provisioning cannot proceed. No profile, app installation, TestFlight build, submission, or production deployment was created. |
| 0.12 Pre-submission control artifacts | staged locally | Draft metadata, App Review notes, screenshot capture plan, TestFlight matrix, and release checklist now preserve the restored editorial identity and list the exact external gates. No App Store Connect record, archive upload, tester invitation, contract, deployment, or submission was created. |
| 0.13 Catalog deployment proof | passed locally; production remains gated | `vercel build` linked only this ignored worktree to the existing project and completed under Node 22. The generated Vercel static output contains byte-identical `index.json`, `cinema.json`, `jazz.json`, and `food.json`. `catalog:artifact:check` makes that build proof deterministic in CI and `catalog:live:check` will require direct HTTPS JSON, CORS, feed integrity, expiry, and provider-policy validation after an owner-authorized deployment. The current production endpoint correctly fails that live gate because `main` has not yet deployed the catalog. |
| 0.14 Current remote release-lane CI | passed | GitHub Quality run [`32174228773`](https://github.com/XAE117/X117/actions/runs/32174228773) passed in 3m02s on `90f5578e0d65e8968e009eeba86e2e328d386f9a`: dependency install, lint, 93 unit tests, production build, rights-gated catalog validation and artifact check, audit, Chromium install, and all 58 route-smoke checks. Vercel Preview also succeeded and PR #14 is clean. GitHub emitted only its runner-level Node 20 deprecation notice for the existing v4 action wrappers; the repository job used Node 22. |
| 2.1 Provider policy | passed | `config/ios-provider-policy.json` defines machine-readable `approved` / `pending` / `disabled` states. Only limited AMC catalog fields and two owner-authored `manualPick` restaurant records are approved. |
| 2.2 Google Places remediation | passed | The restaurant scraper no longer calls Google Places; its geocoder was removed. The deterministic migration stripped legacy Google URLs from 232 restaurant records and 25 guide records, retaining coordinates/hours only on the two documented editorial records. No remote secret was changed. |
| 2.3 Versioned catalog | passed locally | `public/catalog/v1/` contains SHA-256-indexed cinema, food, and explicit-disabled jazz feeds. It contains 5 AMC theatres / 2,368 screenings and 2 SIXPM editorial restaurants; no TMDB, Google, jazz, or unapproved source payload is emitted. The feed will reach production only through the normal reviewed PR/deployment path. |
| 2.4 Catalog verification | passed | 93 unit tests, lint, production build, catalog-current check, catalog validator, and 58 browser checks pass under Node 22. The catalog check is deterministic against the cinema scrape timestamp. |
| 2.5 Current AMC catalog refresh | passed locally | A fresh 2026-08-18T17:40:01.808Z AMC snapshot superseded the earlier 12:28 catalog. Under Node 22, `catalog:build` regenerated the SHA-256-indexed V1 feeds and `catalog:check` passed. The renewed catalog contains 5 AMC theatres / 2,368 screenings, the existing 2 first-party editorial dinner records, disabled Jazz, and no Google or TMDB fields/URLs. |
| 3.1 Separate iPhone entry | passed | `ios.html` and `src/ios/` form a standalone React surface. `vite --mode ios` builds only `dist-ios/`, with no legacy public tree copied into the bundle. |
| 3.2 Remote catalog client | passed locally | The client fetches only versioned catalog paths, verifies SHA-256 digests, field allowlists, expiry, source state, and explicit disabled Jazz behavior before rendering. It cannot fall back to `theaters.json`, `restaurants.json`, or jazz source data. |
| 3.3 Product boundary check | passed | `npm run build:ios` produces an isolated local UI bundle (10 generated files in the current build) and `ios:bundle:check` found no excluded products or API-key variable tokens. |
| 3.4 iPhone-size visual QA | passed locally | At 393×852, the catalog rendered without Vite overlays or console errors; evening-first grouped film cards, food browse/detail, and bottom navigation were exercised. This is browser evidence only, not simulator or device proof. |
| 4.1 Capacitor iPhone shell | passed | Capacitor core/CLI/iOS are `8.5.0`; the generated target uses `com.xae117.sixpm`, iOS 17.0 minimum, and `TARGETED_DEVICE_FAMILY = 1`. The iOS shell loads only `dist-ios/index.html`; the build normalizes the isolated Vite `ios.html` artifact to that Capacitor entrypoint. |
| 4.2 Native capability boundary | passed | Only Browser, Calendar, Geolocation, Local Notifications, Network, Preferences, and Share are present through Swift Package Manager. `NATIVE_CAPABILITIES.md` records the prompt, storage, and no-tracking boundaries. Calendar uses the interactive system editor; location and notification permission denials are never automatically re-prompted. |
| 4.3 Native build verification | passed | `npm audit --omit=dev`, lint, 13 focused iOS tests, `npm run build:ios`, and `cap sync ios` passed. An iPhone simulator build completed with `BUILD SUCCEEDED` using `CODE_SIGNING_ALLOWED=NO`; this is compile evidence only, not signed-device proof. |
| 5.1 Rights-bounded saved evenings | passed | A saved plan contains only an approved AMC showing and first-party food record with catalog provenance. It rejects unapproved providers, missing editorial coordinates, insecure links, stale feeds, and showtimes beyond AMC’s 36-hour freshness window. |
| 5.2 Expiring local storage and offline catalog | passed | A verified catalog may be cached only after full validation; integrity or expiry failure deletes that whole cache. On saved-plan read/write, expired AMC or editorial sections are redacted and rewritten in Preferences rather than silently reused offline. |
| 5.3 Local evening actions | passed in browser wiring | The full choose → pair → save → Calendar/reminder/share → completion/delete flow was exercised at 393×852. Calendar/reminder fallbacks are truthful outside iOS, and deletion requires confirmation. Native share-sheet cancellation and local-notification schedule/cancellation are separately proven in simulator QA; Calendar-editor save/cancel remains separately unproven. |
| 5.4 Phase verification | passed | Under Node 22: `npm run check` passed (80 tests), `npm run catalog:check`, `npm run build:ios`, `npx cap sync ios`, and an iPhone Simulator `xcodebuild` with `CODE_SIGNING_ALLOWED=NO` all passed. |
| 6.1 SIXPM visual identity restoration | passed locally | Historic SIXPM screen captures in the owner's local Photos library and the legacy source supplied the visual authority. The iPhone surface now uses locally bundled Source Serif 4 and Josefin Sans, dense editorial listings, gold frames, venue labels, showtime tags, and deco rules instead of rounded-card UI. At 393×852, browser inspection found no overflow and all visible controls met the 44-point target. An iPhone Simulator inspection used a temporary local, rights-gated catalog solely to make the reviewed data render; the normal release bundle was then rebuilt and re-synced with no localhost endpoint or local-network exception. |
| 6.2 Current normal-bundle verification | passed | Under Node 22: lint, all 85 unit tests, production build, dependency audit, catalog-current/validation, iOS build, Capacitor sync, plist validation, release-bundle local-endpoint guard, diff whitespace check, and an iPhone Simulator `xcodebuild` with `CODE_SIGNING_ALLOWED=NO` all passed. |
| 6.3 Dynamic Type and route focus | passed locally | An app-owned local Capacitor bridge observes iOS Dynamic Type changes and maps categories to proportional custom-font scaling. At the simulator’s `accessibility-large` setting, the masthead stayed intact and dense listings reflowed without clipping; the Simulator was restored to standard `large` afterward. At browser 393×852, route changes move focus to the one `main` landmark, every visible button/link measured at least 44 points, and no horizontal overflow occurred. The temporary rights-gated local catalog and local-network exception used for this native visual proof were removed before the normal-bundle verification. |
| 6.4 Native offline/App Notes proof | passed locally | The current normal release bundle was installed in the booted iPhone 17 / iOS 26.5 simulator. An offline snapshot rendered with the restored editorial surface, App Notes remained reachable, and `accessibility-large` reflow kept the masthead, catalog-status card, and field-index navigation legible. Simulator type size was restored to `large`; no physical-device claim is implied. |
| 6.5 Current-showing and native-action QA | partial, truthful | The iPhone directory now filters already-started LA showings and saved-evening creation rejects them as a second hard guard; 92 unit tests cover both paths. A fresh iPhone Simulator evening was paired and saved, and its native share sheet opened then was cancelled without sending anything. The default 90-minute reminder correctly refused a too-soon showing without prompting. The blank simulator EventKit store has no default writable calendar, so the native Calendar editor returns the documented unavailable state; SIXPM now gives a user-actionable account/setup message instead of exposing a plugin I/O error. Native notification permission/scheduling/cancellation is proven separately at 6.14. Calendar-editor save/cancel with a configured calendar, VoiceOver, reduced motion, and physical-device proof remain unverified. |
| 6.6 Historic directory home restoration | passed locally | Local historic SIXPM captures confirmed that the cinema directory—not a large decision card—is the product's primary visual gesture. The iPhone home now moves from the masthead directly into the compact AMC program, colored venue labels, large serif film titles, showtime/format markers, and a text-only browse affordance; the framed “First Pick” panel was removed. The normal production bundle was rebuilt, compiled, installed, and visually inspected on the iPhone 17 / iOS 26.5 simulator. `npm run check` (92 unit tests), 50 browser checks, `npm run ios:sync`, and the simulator `xcodebuild` all passed. |
| 6.7 Fresh-install catalog handling | partial — production blocker | A disposable fresh iPhone 17 Pro simulator with no catalog cache reached the unavailable state because the live catalog URL resolved to the Vercel SPA HTML. The client now requests explicit string URLs, requires a JSON response before parsing, and shows a concise recovery message rather than a platform parse error; focused catalog tests cover the HTML failure. This prevents unsafe rendering, but a fresh online install cannot show listings until the reviewed deployment serves the versioned catalog. |
| 6.8 Local native catalog QA harness | passed locally | `dev:ios` now returns CORS headers only in iOS development mode, allowing a disposable iPhone 17 Pro Simulator to load a temporary, rights-gated local catalog at `capacitor://localhost`. The restored editorial home rendered natively with the full AMC directory. The temporary `NSAllowsLocalNetworking` exception was removed afterward; the normal iOS bundle was rebuilt, re-synced, and verified free of localhost endpoints and local-network allowance. |
| 6.9 Editorial launch artwork | compiled; visual device proof pending | The generated square splash image was replaced by a native black-and-gold launch storyboard: Baskerville `6PM`, the guide descriptor, and a hairline rule. The storyboard compiles inside the normal simulator build. The Simulator transition did not provide a reliable observable launch-frame capture, so physical-device launch appearance remains explicitly unproven. |
| 6.10 iPhone-shell accessibility regression | passed in browser QA | New iPhone-shell checks load the local rights-gated catalog and verify the five 44-point field-index controls, `aria-current`, route-focus transfer to the `main` landmark, reduced-motion durations, and the non-technical unavailable-catalog state. The checks run on desktop and mobile Chromium; they strengthen UI semantics but do not claim VoiceOver or device proof. |
| 6.11 Directory accessibility semantics | passed in browser QA | The Tonight film and dinner sections now have real heading IDs behind their `aria-labelledby` references, and the Cinema/Dinner selector is an explicitly named control group. Regression checks verify named directory and Notes regions plus nonempty accessible labels and 44-point minimum dimensions for every visible button/link across Tonight, Cinema, Dinner, and Notes in desktop and mobile Chromium. This tightens VoiceOver readiness without claiming a human VoiceOver pass. |
| 6.12 Native catalog-recovery and permission-denial navigation | passed in simulator QA | The explicit disposable-simulator command now uninstalls `com.xae117.sixpm` and resets simulator location privacy before each run, so the test begins with neither an app container nor an offline catalog cache. Against the current public catalog failure (SPA HTML at the catalog URL), it proves the concise unavailable state, tappable 44-point retry control, historical field index, and Notes plus Policies + support. It then taps the app’s user-initiated location control, selects the actual iOS system “Don’t Allow” action, confirms the Settings recovery explanation, and confirms that no automatic re-prompt action remains. `IOS_SIMULATOR_ID=<disposable-simulator-udid> npm run test:ios:ui` makes this proof repeatable without selecting a personal simulator implicitly. It is XCTest/simulator evidence only, not a human VoiceOver or physical-device pass. |
| 6.13 Historic masthead and index restoration | passed locally | A new read-only Photos-library pass recovered the owner’s March 2026 iPhone captures alongside the legacy directory source. The iPhone home now uses the original double-rule/diamond masthead, branded Cinema/Dinner index, tightly ordered showtime rows, and an unboxed five-item bottom field navigation rather than the generic product-dashboard language. At 393×852, browser inspection and a disposable iPhone 17 Pro Simulator render both showed the restored hierarchy with no clipped controls. `npm run check` (93 tests), focused iOS browser smoke (8 desktop/mobile checks), the full `npx playwright test --workers=2` suite (58 checks), `npm run ios:sync`, and `IOS_SIMULATOR_ID=C6FC0BF0-D388-4F32-A3C2-B44A44B519AC npm run test:ios:ui` passed. The Simulator listing render used a temporary local, rights-gated catalog solely for QA; the normal HTTPS-only bundle was immediately rebuilt and checked to contain no localhost endpoint or `NSAllowsLocalNetworking` exception. This remains simulator/browser evidence, not physical-device proof. |
| 6.14 Native local-reminder lifecycle | passed in simulator QA | A disposable `com.xae117.sixpm.qa` build loaded only the local rights-gated catalog, selected a future AMC showing plus the approved editorial dinner, and reached a real iOS notification permission control. XCTest selected “Allow,” confirmed the Capacitor local-notification schedule result, then removed that reminder and confirmed the user-facing cancellation state. The harness also rejects a `Main Thread Checker` diagnostic; the Dynamic Type bridge now returns its UIKit trait collection from the main thread. `IOS_SIMULATOR_ID=<disposable-simulator-udid> npm run test:ios:native-actions` is repeatable: it uninstalls the QA bundle, uses `Info-QA.plist` only for the temporary local-network exception, and always runs the normal `ios:sync` restoration in `finally`. After this run the QA app was absent and the normal bundle contained neither a localhost catalog endpoint nor `NSAllowsLocalNetworking`. This is native simulator proof only; it does not prove notification delivery on a physical iPhone. |
| 7.1 Legal/privacy implementation | passed locally | `/privacy`, `/terms`, `/support`, and `/credits` use the restored SIXPM editorial language, bypass catalog-data failure, and pass desktop/mobile route checks. App Notes includes concise local policy/support summaries and a confirmed erase flow for saved evenings plus the offline catalog. `PrivacyInfo.xcprivacy` parses, is a target resource, and was observed inside the compiled simulator app bundle. Public deployment and App Store metadata entry remain deliberately unperformed. |
| 7.2 App Store privacy/support metadata | owner/external gate | Apple requires a privacy-policy URL for iOS and a Support URL with actual contact information. `APP_STORE_PRIVACY_ANSWERS_DRAFT.md` records the exact remaining decisions; no support email, legal address, or metadata has been invented. |
| 7.3 Hosting telemetry/privacy audit | partial — conservative privacy answer retained | A 2026-08-18 read-only Vercel API/HTML audit found the project's Web Analytics feature disabled, no `@vercel/analytics` or Speed Insights client marker in production or preview HTML, a present-but-empty Speed Insights configuration (`hasData: false`), zero configured log drains, and no Observability Plus entitlement. Those findings rule out app-configured analytics collection, but Vercel's current Privacy Notice says its hosting service processes and retains end-user request, IP-derived coarse location, device, and log metadata. Apple defines collection by whether off-device data is retained beyond real-time servicing. Therefore a blanket “no data collected” App Store answer remains unsafe without an owner-approved classification of the hosting retention path. |
| 8.1 Deterministic iPhone release command | passed locally | `npm run ios:release:check` passed on 2026-08-18 under Node 22: lint, 93 unit tests, production build, dependency audit, 58 browser checks, current iOS catalog validation, exact web build-artifact validation, isolated bundle boundary, privacy manifest, and Capacitor sync. Playwright uses a bounded two-worker pool so Vite’s first-load transforms cannot manufacture parallel route timeouts; the complete desktop/mobile suite passed after that correction. The command separates the App Store lane from the stricter legacy web-guide health thresholds; physical-device proof remains a separate owner-gated requirement. `npm run release:check` now runs its strict diagnostic with `--no-write`, preserving its warning exit without dirtying the worktree. |
| 8.2 Draft release-lane CI | passed | GitHub Quality run `32165174072` passed on commit `444da7701d6e14d43e536b11cdfbd8c201e1d884`: install, lint, 93 unit tests, web build, catalog-current validation, exact catalog-artifact validation, dependency audit, and 58 browser checks. Vercel Preview is Ready. GitHub emitted only its runner-level Node 20 deprecation notice for the existing v4 action wrappers; the repository job itself used Node 22 and no release check failed. |
| 8.3 Current remote CI after native-reminder QA | passed | GitHub Quality run [`32174228773`](https://github.com/XAE117/X117/actions/runs/32174228773) passed in 3m02s on `90f5578`: all required source, catalog, audit, and 58 browser checks succeeded, and Vercel Preview is successful. Native XCTest/simulator evidence is recorded separately at 6.12 and 6.14 rather than implied by this browser/CI run. |

## Phase status

| Phase | State | Completion condition |
| --- | --- | --- |
| 0. Revalidate, integrate, and baseline | complete | Clean worktree, protected canonical tree, current catalog snapshot, Node 22 baseline, audited dependencies, and one draft release lane are established. |
| 1. App Store control documents | complete | This status, scope, execution plan, owner gates, and initial rights ledger are committed. |
| 2. Rights ledger and safe catalog | complete | Machine-enforced provider states prevent pending or disabled data from entering the iOS catalog; legacy Google persistence is remediated and a versioned catalog is generated and verified. |
| 3. iOS product boundary | complete | A separately built local UI consumes only the verified V1 catalog and excludes private/tangential web products from its production bundle. |
| 4. Capacitor and platform adapters | complete | Capacitor 8 iPhone target, local entrypoint, tested platform adapters, truthful purpose strings, and native compile evidence are committed. |
| 5. Saved evenings and offline | complete | Whole-evening plans persist locally, work offline only within their source freshness conditions, support native system actions, and erase provider details when they expire. |
| 6. Accessibility and iPhone polish | in progress | The original SIXPM visual language, historic masthead/index restoration, portrait behavior, safe-area treatment, final icon, compiled editorial launch artwork, Dynamic Type reflow, route focus, minimum target sizing, offline snapshot, future-only showing guard, native share-sheet cancellation, fresh-install catalog-recovery navigation, location-permission denial, and native local-reminder scheduling/cancellation are locally verified. VoiceOver, reduced motion, Calendar with a configured account, physical-device launch/notification proof, and a live versioned catalog endpoint remain. A connected owner iPhone is available, but physical installation currently awaits the owner’s Apple Developer account being signed into Xcode so it can provision `com.xae117.sixpm`. |
| 7. Legal, privacy, and support | staged locally | Legal pages, in-app Notes, deletion controls, and the target privacy manifest are implemented and verified locally. The read-only hosting audit confirms that SIXPM has no configured web analytics, drained logs, or Observability Plus collection, but Vercel platform request-metadata retention still requires a conservative App Store classification. The phase remains uncompleted until the pages are publicly deployed, that classification is owner-approved, and the owner supplies a public App Store support contact. |
| 8. Release checks and device QA | staged locally, not active | The deterministic iPhone command is defined; its proof, device QA, and archive proof remain separate. |
| 9. TestFlight and App Store preparation | staged locally, not active | Metadata, App Review notes, TestFlight plan, screenshot plan, and release checklist are prepared. Native final screenshots, App Store Connect entry, an archive, tester activity, and all related external actions remain gated. |
| 10. Submission and release | blocked by owner gates | Requires exact owner authorization and later manual-release approval. |

## Non-owner blockers being handled autonomously

- Integrate the already-reviewed PR #12 content without overwriting scheduled catalog updates.
- Establish the Node 22 command path and baseline test evidence.
- Build a native-only product surface that consumes the verified catalog without inheriting private or tangential web routes.
- Add Capacitor 8 and native adapters without putting a key, a private product, or a provider-content fallback in the iOS target.
- Keep fresh installs fail-closed and user-legible until the normal reviewed deployment publishes the versioned `catalog/v1/` files.

## Owner and external gates

No provider contact, contract acceptance, fee payment, external TestFlight invitation, production merge/deployment, App Store submission, or release is authorized by this document. The detailed gate list is in [OWNER_GATES.md](OWNER_GATES.md). The required submission authorization is exactly:

```text
SUBMIT SIXPM V1
```

## Evidence discipline

- Browser checks are not native proof.
- Simulator checks are not physical-device proof.
- TestFlight availability is not public App Store proof.
- A provider is unavailable to iOS until the rights registry marks it `approved` with required attribution and freshness conditions.
- No API key may enter the app bundle, `capacitor.config`, Info.plist, screenshots, documentation examples, or public catalog.
